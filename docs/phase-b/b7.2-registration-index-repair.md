# B7.2 Registration & Marketplace Index Normalization Repair

This repair addresses the two remaining B7.2 integration issues:

1. Enterprise Modules registration
   - adds the missing `PackageSearch` Lucide import
   - registers `/app/marketplace/catalog` under Suppliers
   - leaves the already-created access-registry metadata unchanged

2. Prisma/PostgreSQL index-name normalization
   - renames three existing marketplace indexes
   - does not recreate tables or indexes
   - does not change marketplace data

Migration:
`20260808081500_b7_marketplace_normalize_index_names`

After applying the patch:
- run the registration repair script
- run `npx prisma migrate dev`
- confirm `prisma migrate diff` is empty
