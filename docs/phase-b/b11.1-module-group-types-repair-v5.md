# B11.1 Module Group Types Repair v5

The previous repair targeted `src/core/modules/registry.ts`, but the actual
`ModuleGroup` type is declared in:

`src/core/modules/types.ts`

`registry.ts` imports `ModuleRegistryEntry` from that file, and
`ModuleRegistryEntry.group` is typed as `ModuleGroup`.

This repair adds `"Automation"` directly to the real `ModuleGroup` union.

No Prisma schema changes.
No migration.
No database operation.
