# B13.10.13b — Partial Install Repair

This repair is specifically for a working tree where B13.10.13a was applied and
its integration script ran only partially.

It fixes:

- stale `temporaryPassword: true` in `updateMembershipSchema.omit`
- accidental schema inference collapse affecting `updateMembershipAction`
- partially modified tenant-user activation service signature
- missing safe reset/resend behavior
- missing invalidation of prior unused activation tokens
- missing activation email success/failure audit evidence
- legacy invitation function behavior

The existing `/activate-user-account` page, activation form and
`consumeTenantUserActivation` flow are preserved.

No Prisma migration is required.
