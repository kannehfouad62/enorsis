# B11.1 Module Registry Group Repair v4

After adding `Automation` to `EnterpriseModuleLink.group`, the core module
registry still rejected that value because its separate `ModuleGroup` union
did not include `Automation`.

This repair updates only `src/core/modules/registry.ts` so the registry and
navigation module-group types remain aligned.

No Prisma schema change.
No migration.
No database operation.
