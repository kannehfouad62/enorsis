# B2.9.2.1 — Visual Rule Builder Foundation

Adds:
- rule designer state
- nested AND/OR condition model
- advanced condition operators
- action palette
- reusable system templates
- immutable rule version history
- validation reports
- simulation input and execution preview
- publish/supersede version lifecycle
- visual rule builder workspace

Route:

```text
/app/automation/designer
```

Designer state is stored as governed JSON so future drag/drop node types can
evolve without repeated schema migrations.
