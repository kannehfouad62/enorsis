# B8.2 Authorization Repair v3

The v2 repair successfully removed the invalid role values but appended the
replacement roles to a locally one-line role declaration, resulting in a
missing comma before `RISK_COMPLIANCE`.

This v3 repair replaces the entire:

`const roles = [...] as const;`

declaration with a known-valid Enorsis role list.

No schema, migration, forecasting, inventory, or recommendation logic changes
are made.
