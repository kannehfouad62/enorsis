# Enorsis Inventory and Materials Management

This module adds inventory locations, stock items, balances, movements,
reorder controls, lot and serial foundations, cycle counts, valuation, and
low-stock indicators.

Run:

```bash
python3 scripts/update-enterprise-modules-for-inventory.py
```

to add the module to the Procurement section of `/app/modules`.
