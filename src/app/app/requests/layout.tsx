import { BuyerCommercialLayout } from "@/components/commercial-access/BuyerCommercialLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <BuyerCommercialLayout>{children}</BuyerCommercialLayout>;
}
