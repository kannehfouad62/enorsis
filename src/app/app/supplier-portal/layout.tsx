import { SupplierCommercialLayout } from "@/components/commercial-access/SupplierCommercialLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <SupplierCommercialLayout>{children}</SupplierCommercialLayout>;
}
