"use client";import {useMemo,useState} from "react";
const rates:Record<string,number>={USD:1,EUR:.92,GBP:.78,CAD:1.36,AUD:1.51,NGN:1550,GHS:15.2,KES:129,ZAR:18.3,AED:3.67};
export function CurrencySelector({amount=125000}:{amount?:number}){const [currency,setCurrency]=useState('USD');const converted=useMemo(()=>amount*rates[currency], [amount,currency]);return <div className="glass rounded-2xl p-5"><div className="flex items-center justify-between gap-4"><div><p className="text-xs uppercase tracking-[.18em] text-slate-400">Global spend view</p><p className="mt-1 text-3xl font-black">{new Intl.NumberFormat('en-US',{style:'currency',currency,maximumFractionDigits:0}).format(converted)}</p><p className="text-xs text-slate-500">Base ledger: ${amount.toLocaleString()} USD</p></div><select value={currency} onChange={e=>setCurrency(e.target.value)} className="rounded-xl border border-white/10 bg-[#111933] px-3 py-2">{Object.keys(rates).map(c=><option key={c}>{c}</option>)}</select></div><label className="mt-4 flex items-center gap-2 text-sm text-slate-300"><input type="checkbox"/> Set as organization default currency</label></div>}
